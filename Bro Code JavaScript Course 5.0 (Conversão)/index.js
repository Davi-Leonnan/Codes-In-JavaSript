// Conversão de tipos de variáveis (strings, numbers, booleans) no JavaScript:

// Converter valor string para valor number:

/*let age = window.prompt("Quantos anos você tem?: ");

age = Number(age);
age+=1;

console.log(age, typeof age);
*/

// Oitros tipos de conversão:

let x = "coxinha";
let y = "coxinha";
let z = "coxinha";

x = Number(x);
y = String(y);
z = Boolean(z);

console.log(x, typeof x);
console.log(y, typeof y);
console.log(z, typeof z);





