// Adição da bliblioteca "math" para operações matemáticas no JavaScript:


// Valor de "pi":

console.log(Math.PI);

// valor de Euler (E):

console.log(Math.E); 

let t = 10;
let u;
let v;
let w = 6.33;
let x = 16;
let y = 2.21;
let z;

let a = 67;
let b = 68;
let c = 69;

// Arredondamento de um valor numérico (Valor aproximado):

z = Math.round(x);

console.log(z);

// Arredondamento de um valor numérico (Pra menor):

v= Math.floor(w);

console.log(v); 

// Arredondamento de um valor numérico (Pra maior):

u= Math.ceil(y);

console.log(u);

// Exponencial de um valor de uma variável elevado a um valor de outra.

z = Math.pow(x, y);

console.log(z);

// Raiz quadrada de um valor:

z = Math.sqrt(x);

console.log(z);

// Logaritimo de um valor:

console.log(Math.log(v));

// Seno, Cosseno e Tangente:

z = Math.sin(x);
z = Math.cos(x);
z = Math.tan(x);

console.log(z)

// Valor máximo e mínimo entre múltiplos valores:

let max = Math.max(a, b, c);
let min = Math.min(a, b, c);

console.log(max);
console.log(min);







